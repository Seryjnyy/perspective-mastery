import React from "react";

export default function page() {
  return (
    <div>
      <span>
        Well you can do this in blender or any other 3D modeling software
      </span>
      <span>
        well of course you can, but its extra resistance that you don't need
        when your facing something as tedious as learning perspective.
      </span>
    </div>
  );
}
